import React, { Component } from "react";



export default function NavigationBarView(props) {

    return (
        <View>

            <button onClick={props.onProfileClick}></button>
        </View>)
}
